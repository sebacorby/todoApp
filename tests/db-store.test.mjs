import test from "node:test";
import assert from "node:assert/strict";
import { mkdirSync, mkdtempSync, readFileSync, rmSync, existsSync } from "node:fs";
import { dirname, join } from "node:path";
import { tmpdir } from "node:os";
import { DatabaseSync } from "node:sqlite";
import { TodoStore, databasePath, DEFAULT_COLORS, SCHEMA_VERSION } from "../electron/db-store.js";
const fixture=()=>{const root=mkdtempSync(join(tmpdir(),"todo-db-"));return{root,file:databasePath(root)}};
const clean=(root,s)=>{s?.close();rmSync(root,{recursive:true,force:true})};
const task=(title="T")=>{const now="2026-08-20T10:00:00.000Z";return{title,description:"",startsAt:"2026-08-20T12:00:00.000Z",endsAt:"2026-08-20T13:00:00.000Z",backlogOrder:null,backlogGroupId:null,status:"not_started",criticality:"medium",recurrence:"none",recurrenceEnd:null,completedAt:null,createdAt:now,updatedAt:now}};
const backlog=(title="B",order=0)=>({...task(title),startsAt:null,endsAt:null,backlogOrder:order});
test("creates physical SQLite current schema",()=>{const {root,file}=fixture(),s=new TodoStore(file);assert.ok(existsSync(file));assert.equal(readFileSync(file).subarray(0,16).toString("utf8"),"SQLite format 3\0");assert.equal(s.db.prepare("PRAGMA user_version").get().user_version,SCHEMA_VERSION);clean(root,s)});
test("CRUD and settings persist",()=>{const {root,file}=fixture();let s=new TodoStore(file);const id=s.saveTask(task("Persistente"));s.setSetting("criticalityColors",{...DEFAULT_COLORS,urgent:"#abcdef"});s.close();s=new TodoStore(file);assert.equal(s.getTask(id).title,"Persistente");assert.equal(s.getSetting("criticalityColors").urgent,"#abcdef");clean(root,s)});
test("backlog persists null dates and order",()=>{const {root,file}=fixture(),s=new TodoStore(file);const id=s.saveTask(backlog("Sin fecha",7));assert.equal(s.getTask(id).startsAt,null);assert.equal(s.getTask(id).backlogOrder,7);clean(root,s)});
test("migrates v1 to current schema without data loss",()=>{const {root,file}=fixture();mkdirSync(dirname(file),{recursive:true});const raw=new DatabaseSync(file);raw.exec(`CREATE TABLE tasks(id INTEGER PRIMARY KEY AUTOINCREMENT,title TEXT NOT NULL,description TEXT NOT NULL DEFAULT '',starts_at TEXT NOT NULL,ends_at TEXT NOT NULL,status TEXT NOT NULL,criticality TEXT NOT NULL,recurrence TEXT NOT NULL DEFAULT 'none',recurrence_end TEXT,completed_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL);CREATE TABLE settings(key TEXT PRIMARY KEY,value_json TEXT NOT NULL,updated_at TEXT NOT NULL);INSERT INTO tasks(title,description,starts_at,ends_at,status,criticality,recurrence,created_at,updated_at) VALUES('Vieja','','2026-08-20T12:00:00.000Z','2026-08-20T13:00:00.000Z','not_started','medium','none','2026-08-20T10:00:00.000Z','2026-08-20T10:00:00.000Z');PRAGMA user_version=1;`);raw.close();const s=new TodoStore(file);assert.equal(s.db.prepare("PRAGMA user_version").get().user_version,SCHEMA_VERSION);assert.equal(s.allTasks()[0].title,"Vieja");assert.equal(s.allTasks()[0].backlogGroupId,null);clean(root,s)});
test("future schema is rejected",()=>{const {root,file}=fixture();let s=new TodoStore(file);s.close();const raw=new DatabaseSync(file);raw.exec(`PRAGMA user_version=${SCHEMA_VERSION+1}`);raw.close();assert.throws(()=>new TodoStore(file),/newer than supported/);rmSync(root,{recursive:true,force:true})});
test("backlog recurrence and scheduled group assignment are rejected",()=>{const {root,file}=fixture(),s=new TodoStore(file);assert.throws(()=>s.saveTask({...backlog(),recurrence:"daily"}),/cannot be recurrent/i);assert.throws(()=>s.saveTask({...task(),backlogGroupId:1}),/cannot belong/i);clean(root,s)});
