{
  "name": "todo-app",
  "productName": "TodoApp",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "smoke": "electron . --smoke-test",
    "smoke:cai": "electron . --smoke-test --no-sandbox",
    "smoke:renderer": "electron . --renderer-smoke-test --no-sandbox",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "electron": "43.4.1"
  }
}
